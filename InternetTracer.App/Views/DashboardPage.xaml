<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InternetTracer_App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:InternetTracer_App.Converters"
    xmlns:core="using:InternetTracer.Core.Contracts"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:ByteFormatValueConverter x:Key="ByteValueConverter" />
        <converters:ByteFormatUnitConverter x:Key="ByteUnitConverter" />
    </Page.Resources>

    <ScrollViewer>
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="Auto" /> <!-- Top KPIs -->
                <RowDefinition Height="Auto" /> <!-- Top Apps -->
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Overview" Style="{StaticResource TypographyPageTitle}" />

            <!-- KPI Row -->
            <Grid Grid.Row="1" ColumnSpacing="16" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Current Download KPI -->
                <Border Grid.Column="0" Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="CURRENT DOWNLOAD" Style="{StaticResource TypographySectionTitle}" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentDownloadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" 
                                       Style="{StaticResource TypographyMetric}" 
                                       Foreground="{ThemeResource TelemetryDownloadBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentDownloadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteUnitConverter}}" 
                                       Style="{StaticResource TypographySecondary}" Margin="8,0,0,6" VerticalAlignment="Bottom" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Current Upload KPI -->
                <Border Grid.Column="1" Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="CURRENT UPLOAD" Style="{StaticResource TypographySectionTitle}" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentUploadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" 
                                       Style="{StaticResource TypographyMetric}" 
                                       Foreground="{ThemeResource TelemetryUploadBrush}"/>
                            <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentUploadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteUnitConverter}}" 
                                       Style="{StaticResource TypographySecondary}" Margin="8,0,0,6" VerticalAlignment="Bottom" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Connection Quality KPI -->
                <Border Grid.Column="2" Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="CONNECTION QUALITY" Style="{StaticResource TypographySectionTitle}" />
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <TextBlock Text="{x:Bind ViewModel.Quality.Status, Mode=OneWay}" 
                                       Style="{StaticResource TypographyMetric}" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
            
            <Border Grid.Row="2" Style="{StaticResource SurfacePrimaryStyle}">
                <StackPanel>
                    <TextBlock Text="TOP APPLICATIONS (TODAY)" Style="{StaticResource TypographySectionTitle}" />
                    
                    <ItemsRepeater ItemsSource="{x:Bind ViewModel.Summary.TopApplications, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="core:TopUsageEntry">
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Grid.Column="0" Text="{x:Bind DisplayName}" VerticalAlignment="Center" FontFamily="{StaticResource BodyFontFamily}" />
                                    
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,16,0" VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind TotalBytes, Converter={StaticResource ByteValueConverter}}" FontWeight="SemiBold" />
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{x:Bind TotalBytes, Converter={StaticResource ByteUnitConverter}}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </StackPanel>
            </Border>
            
            <ProgressRing Grid.Row="2" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </ScrollViewer>
</Page>
