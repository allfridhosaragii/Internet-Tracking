<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InternetTracer_App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:InternetTracer_App.Converters"
    xmlns:core="using:InternetTracer.Core.Contracts"
    xmlns:components="using:InternetTracer_App.Components"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <converters:ByteFormatValueConverter x:Key="ByteValueConverter" />
        <converters:ByteFormatUnitConverter x:Key="ByteUnitConverter" />
    </Page.Resources>

    <components:DataStateContainer 
        State="{x:Bind ViewModel.DashboardDataState, Mode=OneWay}"
        ErrorMessage="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
        OfflineMessage="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
        HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

        <ScrollViewer>
            <StackPanel Margin="32" Spacing="24">
                
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup>
                        <!-- Wide Layout -->
                        <VisualState x:Name="WideLayout">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="1024" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="TrafficGrid.ColumnDefinitions[0].Width" Value="*" />
                                <Setter Target="TrafficGrid.ColumnDefinitions[1].Width" Value="*" />
                                <Setter Target="NetworkGrid.ColumnDefinitions[0].Width" Value="*" />
                                <Setter Target="NetworkGrid.ColumnDefinitions[1].Width" Value="*" />
                                <Setter Target="UploadCard.(Grid.Column)" Value="1" />
                                <Setter Target="UploadCard.(Grid.Row)" Value="0" />
                                <Setter Target="ActiveConnectionsCard.(Grid.Column)" Value="1" />
                                <Setter Target="ActiveConnectionsCard.(Grid.Row)" Value="0" />
                                <Setter Target="UploadCard.Margin" Value="16,0,0,0" />
                                <Setter Target="ActiveConnectionsCard.Margin" Value="16,0,0,0" />
                            </VisualState.Setters>
                        </VisualState>
                        <!-- Narrow Layout -->
                        <VisualState x:Name="NarrowLayout">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <!-- Stack vertically -->
                                <Setter Target="TrafficGrid.ColumnDefinitions[0].Width" Value="*" />
                                <Setter Target="TrafficGrid.ColumnDefinitions[1].Width" Value="0" />
                                <Setter Target="NetworkGrid.ColumnDefinitions[0].Width" Value="*" />
                                <Setter Target="NetworkGrid.ColumnDefinitions[1].Width" Value="0" />
                                
                                <Setter Target="UploadCard.(Grid.Column)" Value="0" />
                                <Setter Target="UploadCard.(Grid.Row)" Value="1" />
                                <Setter Target="ActiveConnectionsCard.(Grid.Column)" Value="0" />
                                <Setter Target="ActiveConnectionsCard.(Grid.Row)" Value="1" />
                                
                                <Setter Target="UploadCard.Margin" Value="0,16,0,0" />
                                <Setter Target="ActiveConnectionsCard.Margin" Value="0,16,0,0" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>

                <!-- 1. LIVE STATE HEADER -->
                <Grid ColumnSpacing="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="LIVE SYSTEM STATE" Style="{StaticResource TypographyPageTitle}" Margin="0"/>
                    
                    <!-- Freshness Indicator -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Spacing="8">
                        <Ellipse Width="10" Height="10" Fill="{ThemeResource TelemetryDownloadBrush}">
                            <Ellipse.Triggers>
                                <EventTrigger RoutedEvent="Ellipse.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:1" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Ellipse.Triggers>
                        </Ellipse>
                        <TextBlock Text="{x:Bind ViewModel.LastUpdatedText, Mode=OneWay}" Style="{StaticResource TypographySecondary}" />
                    </StackPanel>
                </Grid>

                <!-- 2. TRAFFIC -->
                <Grid x:Name="TrafficGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource SurfacePrimaryStyle}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="DOWNLOAD" Style="{StaticResource TypographySectionTitle}" />
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentDownloadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" 
                                           Style="{StaticResource TypographyDisplay}" 
                                           Foreground="{ThemeResource TelemetryDownloadBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentDownloadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteUnitConverter}}" 
                                           Style="{StaticResource TypographySecondary}" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                            </StackPanel>
                            <TextBlock Text="{x:Bind ViewModel.Summary.TodayTraffic.DownloadBytes, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" Style="{StaticResource TypographySecondary}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border x:Name="UploadCard" Grid.Column="1" Grid.Row="0" Style="{StaticResource SurfacePrimaryStyle}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="UPLOAD" Style="{StaticResource TypographySectionTitle}" />
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentUploadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" 
                                           Style="{StaticResource TypographyDisplay}" 
                                           Foreground="{ThemeResource TelemetryUploadBrush}"/>
                                <TextBlock Text="{x:Bind ViewModel.Snapshot.CurrentUploadBytesPerSec, Mode=OneWay, Converter={StaticResource ByteUnitConverter}}" 
                                           Style="{StaticResource TypographySecondary}" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                            </StackPanel>
                            <TextBlock Text="{x:Bind ViewModel.Summary.TodayTraffic.UploadBytes, Mode=OneWay, Converter={StaticResource ByteValueConverter}}" Style="{StaticResource TypographySecondary}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 3. APPLICATION ACTIVITY -->
                <Border Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="TOP APPLICATIONS (TODAY)" Style="{StaticResource TypographySectionTitle}" />
                        
                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.Summary.TopApplications, Mode=OneWay}" Margin="0,8,0,0">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="core:TopUsageEntry">
                                    <Grid Margin="0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        
                                        <TextBlock Grid.Column="0" Text="{x:Bind DisplayName}" Style="{StaticResource TypographyDataValue}" VerticalAlignment="Center" />
                                        
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <TextBlock Text="{x:Bind TotalBytes, Converter={StaticResource ByteValueConverter}}" Style="{StaticResource TypographyDataValue}" />
                                            <TextBlock Text="{x:Bind TotalBytes, Converter={StaticResource ByteUnitConverter}}" Style="{StaticResource TypographyCaption}" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                </Border>

                <!-- 4. NETWORK / CONNECTION -->
                <Grid x:Name="NetworkGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource SurfacePrimaryStyle}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="CONNECTION QUALITY" Style="{StaticResource TypographySectionTitle}" />
                            <TextBlock Text="{x:Bind ViewModel.Quality.Status, Mode=OneWay}" Style="{StaticResource TypographyMetric}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                    
                    <Border x:Name="ActiveConnectionsCard" Grid.Column="1" Grid.Row="0" Style="{StaticResource SurfacePrimaryStyle}" Margin="0">
                        <StackPanel>
                            <TextBlock Text="ACTIVE CONNECTIONS" Style="{StaticResource TypographySectionTitle}" />
                            <TextBlock Text="{x:Bind ViewModel.Snapshot.ActiveConnections, Mode=OneWay}" Style="{StaticResource TypographyMetric}" Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <!-- 5. HISTORY / CONTEXT (Placeholder for K4 visualizer) -->
                <Border Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="TRAFFIC TIMELINE" Style="{StaticResource TypographySectionTitle}" />
                        <TextBlock Text="[Live Visualization reserved for K4 Phase]" Style="{StaticResource TypographySecondary}" Margin="0,24" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- 6. ANOMALIES -->
                <Border Style="{StaticResource SurfacePrimaryStyle}">
                    <StackPanel>
                        <TextBlock Text="OBSERVATIONS" Style="{StaticResource TypographySectionTitle}" />
                        <TextBlock Text="No active network anomalies detected." Style="{StaticResource TypographySecondary}" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </components:DataStateContainer>
</Page>
